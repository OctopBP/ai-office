# Что за проект

**AI Office** — приложение, где команда AI-агентов визуализирована как офис с
видом сверху. Пользователь ставит задачу менеджеру, тот раздаёт её исполнителям,
человечки в комнате показывают, кто чем занят.

Сейчас команда работает **над самим этим приложением**. То есть проект, который
вы правите, — это тот же офис, в котором вы сидите.

## Главный принцип

> Офис — это визуализация, а не источник правды.

Источник правды — доска задач и лента событий. Ходьба, пузыри и анимации
рендерятся **из** событий и ничего не блокируют. Поэтому графику можно выключить,
и всё продолжит работать. Не заводите логику в компонентах отрисовки.

## Стек

Node + TypeScript, React 19 + Zustand на фронте, `ws` для связи, Vite для сборки.
Движок агентов — `@anthropic-ai/claude-agent-sdk`. Хранилище — JSON-файл, не БД:
данных мало, а нативный модуль вроде better-sqlite3 добавил бы риск сборки.

## Карта файлов

| Файл | Ответственность |
|---|---|
| `src/server/agents.ts` | Сессии менеджера и исполнителей, MCP-инструменты, разбор событий SDK |
| `src/server/state.ts` | Состояние офиса, задачи, шина событий |
| `src/server/roles.ts` | Роли: промпт, модель, лимит клонов, изоляция |
| `src/server/offices.ts` | Реестр офисов: офис = проект со своей директорией и состоянием |
| `src/server/permissions.ts` | Классификатор риска вызова инструмента |
| `src/server/mcp.ts` | Внешние MCP-серверы ролей: каталог и умолчания по ролям |
| `src/server/packages.ts` | Пакеты агентов: манифест `agent.json`, брифы, валидатор, роль из пакета (`packages/@office/<роль>/`) |
| `src/server/skills.ts` | Что из пакета уезжает в сессию: скилы, плагины, просьбы о серверах |
| `src/server/market.ts` | Маркет агентов: реестр `registry/registry.json`, установка из git в кеш `~/.office/packages`, найм из пакета, обновления |
| `src/server/export.ts`, `src/server/publish.ts`, `src/server/bench.ts`, `src/cli/office-agent.ts` | Авторский путь: экспорт роли в пакет, заготовка, валидатор, стенд, публикация в реестр, проверка реестра |
| `src/registry/service.ts`, `src/registry/main.ts` | Сервис индекса: реестр по адресу, поиск, страницы, зеркало с отпечатком, публикация по GitHub-токену, админ, marketplace.json |
| `src/server/limits.ts` | Лимиты плана подписки: проценты и время сброса из событий SDK |
| `src/server/outcomes.ts`, `src/shared/report.ts` | Живой офис: исход задачи (чисто, с доработкой, вставала, провал, снята, откачена), поиск откатов по git, табель роли |
| `src/server/journal.ts`, `src/server/questions.ts` | Журнал офиса (записи, области, забывание) и вопросы владельцу (`ask_owner`, ответы в журнал, порция для планёрки) |
| `src/server/rituals.ts` | Ритуалы офиса на тихом тике надзора: планёрка из данных, консолидация, противоречия и рефлексия через `RitualAgents`, забывание, здоровье проекта, порог лимита |
| `src/server/initiatives.ts`, `src/server/selfchange.ts` | Инициативы: фича от офиса по режиму (`off`/`propose`/`auto`) и доле на своё; предложения владельцу и их применение (правило в бриф, настройка из белого списка) |
| `src/server/market.ts` → `tellOutcome`, `src/registry/service.ts` → `/outcome` | Репутация пакета: исходы задач по согласию (галочка телеметрии) считаются сервисом в долю чистых закрытий и цену; видна на карточке маркета |
| `src/server/git.ts` | Ветки, worktree, коммиты, слияние |
| `src/server/premerge.ts`, `src/server/checks.ts`, `scripts/premerge.ts` | Пред-merge гейт: чистая рабочая копия, пробное слияние, набор дешёвых проверок на слитом дереве — и только потом слияние. Им же сливает конвейер офиса (`review.ts` → узел merge); набор задаётся `OFFICE_MERGE_CHECKS` в premerge.ts, настройкой `Settings.mergeChecks` или одноимённой переменной окружения |
| `src/server/overlap.ts` | Дублирующие правки: файлы и символы, которые после точки ветвления правили и основная ветка, и ветка задачи. Предупреждение гейта, слияние не блокирует (`npm run test:overlap`) |
| `src/server/cloud.ts` | Облачный режим на Managed Agents (написан, вживую не проверен) |
| `src/server/index.ts` | HTTP + WebSocket, раздача собранного веба, команды от клиента |
| `src/shared/types.ts` | Контракт между сервером и вебом |
| `src/shared/looks.ts` | Внешности агентов: скины персонажа и запасные спрайты для аватарок |
| `src/web/office3d/Office3D.tsx` | Трёхмерный рендер комнаты и человечков |
| `src/web/shell/Shell.tsx` | Оболочка: рейл офисов, сегменты видов, композер |
| `src/web/styles/tokens.css`, `kit.css` | Токены и кит нового UI; стенд — `?kit=1` |
| `src/web/MoneyBoard.tsx` | Доска расходов: счёт за день, по задачам, шкалы лимитов |
| `design/sprites/gen.py` | Генератор пиксель-спрайтов (темы day и night) |

## Несколько репозиториев

**В этом проекте все роли работают в одном репозитории** — и код, и графика, и
документы. Разделять его не надо и не предлагайте: `src/server` и `src/web`
связаны общим контрактом `src/shared/types.ts`, спрайты из `design/` попадают в
сборку веба через `import.meta.glob`, а дизайнеру нужно читать `Office3D.tsx` —
именно слепота к исходникам испортила первую задачу про планировку.

Механизм ниже существует для проектов, которые правда живут в нескольких
репозиториях.

У роли может быть свой репозиторий (поле «Репозиторий роли»). Тогда ветка задачи,
её diff и слияние идут туда, а не в общий репозиторий офиса. Репозиторий
фиксируется на задаче в момент старта — `task.repoDir`; всё, что потом делается с
результатом, обязано брать путь оттуда, а не из настройки роли. Одна задача живёт
ровно в одном репозитории.

Если нужно знать, как устроена часть другой роли, — `ask_colleague({role, question})`.
Отвечает живая сессия этой роли в её репозитории, только на чтение. Лезть в чужой
репозиторий без крайней необходимости не надо даже на чтение.

## Правила работы в этом проекте

- **Комментарии и текст интерфейса — по-русски.** Комментарий объясняет «почему
  так», а не пересказывает код.
- **Перед сдачей задачи прогоняйте `npm run typecheck`.** Зависимости в вашей
  рабочей копии есть — `node_modules` подключён симлинком.
- **Не трогайте `src/shared/types.ts` мимоходом**: это общий контракт сервера и
  веба. Меняете — скажите об этом в отчёте, иначе соседняя ветка сломается при
  слиянии.
- **Промпты — это код.** Правка `PM_PROMPT` или `brief` роли меняет поведение
  офиса сильнее, чем правка функции. Изменили — прогоните `npm run test:pm`.
- Не пишите заглушки вместо рабочего кода.
- Не создавайте файлы «на будущее» и не переписывайте то, о чём не просили.

## Как запускается

```
npm run dev      # разработка: сервер с watch + vite на :5173
npm run office   # как приложение: сборка + сервер со статикой на :3001
npm run typecheck
npm run test:pm  # регрессия поведения менеджера (исполнители заглушены)

# Пред-merge гейт: чистая копия → пробное слияние → проверки на слитом дереве
# → и только потом слияние. Красное — не сливает и печатает файлы и ошибки.
npm run premerge -- --branch task/T-142            # проверить и слить
npm run premerge -- --branch task/T-142 --check-only  # только проверить
npm run premerge -- --branch task/T-142 --stash     # свои правки убрать в stash
```

Гейт заодно печатает «проверь дубль правки»: файлы (и функции в них), которые
после точки ветвления правили обе стороны. Конфликта там нет — git слил молча, —
но именно так расходятся две независимые починки одного места. Слияние это
не останавливает.

## Чего в проекте сознательно нет

Аутентификации, мультиюзера, облачного хранения, редактора планировки офиса,
автоматического слияния веток. Это осознанные решения, а не недоделки.
